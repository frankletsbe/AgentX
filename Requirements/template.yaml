Prompt_Template: |
  ROLE: {role}
  
  TASK: {task}
  
  REQUIREMENTS:
  {requirements}

  TOOLS AVAILABLE:
  {tools_description}
  
  SEARCH INSTRUCTIONS:
  {search_instructions}
  
  OUTPUT FORMAT:
  {output_format}